# 1、常用命令：

```bash
ag "string" --ignore-dir <dir>                 # 排除目录
ag -i "string"                                 # 大小写不敏感
ag -w "string"                                 # 全词匹配(大小写敏感)

# 正则表达式
[^ABC]                                         # 匹配出来[...]里的字符
```
