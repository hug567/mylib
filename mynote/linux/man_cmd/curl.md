# 1、常见用法

```bash
# 下载文件
curl https://file.tar.gz -o file.tar.gz

# 下载文件指定账号密码
curl -u user:passed https://file.tar.gz -o file.tar.gz

# 使用代理
curl -x 192.168.0.1:8888 https://file.tar.gz -o file.tar.gz
```


