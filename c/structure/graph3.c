/* 无向图，边带权，十字链表 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_INT (int)((~0U) >> 1)
#define MIN_INT (int)(1 << (8 * sizeof(int) - 1))
#define MAX_SIZE 100

int main()
{
	printf("hello graph3\n");
	return 0;
}
