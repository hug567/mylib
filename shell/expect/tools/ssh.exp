#!/usr/bin/expect -f

set timeout 10

set user jenkins
set ip 10.116.36.44
set port 1000
set password jenkins

spawn ssh -p$port $user@$ip
expect {
    "*yes/no" { send "yes\r"; exp_continue}
    "*password:" { send "$password\r" }
}

interact
